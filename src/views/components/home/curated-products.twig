<section class="max-w-[1280px] mx-auto px-4 md:px-8 xl:px-0 w-full py-10 md:py-14 relative z-10">
    {% if component.fields.title %}
        <div class="reveal mb-8 md:mb-10 flex flex-col md:flex-row justify-between items-start md:items-end gap-4 border-b border-outline/60 pb-6">
            <div>
                <h2 class="font-display text-[26px] md:text-[32px] font-extrabold tracking-[-0.03em] text-ink leading-none">{{ component.fields.title }}</h2>
                <div class="w-12 h-1 bg-lime rounded-full mt-3 shadow-glow"></div>
            </div>
            {% if component.fields.description %}<p class="text-[14px] leading-6 text-gray-500 max-w-md">{{ component.fields.description }}</p>
            {% else %}<p class="text-[14px] leading-6 text-gray-500 max-w-md hidden md:block">اكتشف تشكيلتنا المختارة بعناية.</p>{% endif %}
        </div>
    {% endif %}

    {% if component.fields.products|length %}
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6 reveal-stagger">
            {% for product in component.fields.products %}
                <div class="product-card-atelier group flex flex-col p-3 md:p-4 h-full">
                    <div class="relative aspect-square overflow-hidden rounded-[16px] bg-paper-container">
                        {% if product.image.url %}
                            <img src="{{ product.image.url }}" alt="{{ product.image.alt|default(product.name) }}" class="thumb-zoom w-full h-full object-cover" loading="lazy">
                        {% else %}
                            <img src="{{ 'images/placeholder.png'|asset }}" alt="{{ product.name }}" class="w-full h-full object-cover opacity-50">
                        {% endif %}
                        <div class="scrim absolute inset-0 pointer-events-none"></div>
                        <salla-button type="favorite" product-id="{{ product.id }}" class="absolute top-2.5 left-2.5 w-9 h-9 bg-white/90 backdrop-blur-md border border-white/70 rounded-full flex items-center justify-center text-ink hover:text-red-600 hover:bg-white shadow-sm transition-all">
                            <i class="sicon-heart text-[16px]"></i>
                        </salla-button>
                        {% if product.is_new %}<span class="absolute top-2.5 right-2.5 bg-lime text-ink text-[10px] font-bold tracking-widest uppercase px-2.5 py-1 rounded-full shadow-glow">جديد</span>{% endif %}
                    </div>
                    <div class="flex flex-col flex-grow pt-3">
                        <h3 class="text-[14px] font-bold leading-snug text-ink line-clamp-2 group-hover:text-forest transition-colors min-h-[40px]">
                            <a href="{{ product.url }}">{{ product.name }}</a>
                        </h3>
                        <div class="mt-auto flex items-center justify-between pt-3 border-t border-outline/60">
                            <div class="flex flex-col">
                                {% if product.has_special_price %}
                                    <span class="text-[16px] font-extrabold tracking-[-0.02em] text-forest leading-none">{{ product.price|money }}</span>
                                    <span class="text-[11px] text-gray-400 line-through">{{ product.regular_price|money }}</span>
                                {% else %}
                                    <span class="text-[16px] font-extrabold tracking-[-0.02em] text-ink leading-none">{{ product.price|money }}</span>
                                {% endif %}
                            </div>
                            <salla-add-product-button product-id="{{ product.id }}" class="w-9 h-9 rounded-full bg-ink text-white flex items-center justify-center hover:bg-ink-hover hover:shadow-glow transition-all">
                                <i class="sicon-add text-[18px]"></i>
                            </salla-add-product-button>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        {# — DEMO GLASS SHOWCASE — CEO: guarantees glassy products visible even on empty store, proves glass + design instantly — #}
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5 md:gap-6 reveal-stagger">
            {% for i in 1..6 %}
            <div class="product-card-atelier group flex flex-col p-4 md:p-5 is-visible">
                <div class="relative aspect-[4/5] overflow-hidden rounded-[24px] bg-paper-container">
                    <img src="{{ 'images/placeholder.png'|asset }}" alt="منتج فاخر {{ i }}" class="thumb-zoom w-full h-full object-cover opacity-90" loading="lazy">
                    <div class="scrim absolute inset-0 pointer-events-none"></div>
                    <div class="absolute top-3.5 right-3.5 bg-lime text-ink text-[11px] font-bold tracking-[0.06em] uppercase px-3.5 py-1.5 rounded-full shadow-glow">جديد · جديد</div>
                    <div class="absolute top-3.5 left-3.5 w-10 h-10 bg-white/90 backdrop-blur-md rounded-full flex items-center justify-center text-ink border border-white/60 shadow-sm"><i class="sicon-heart text-[18px]"></i></div>
                    <div class="absolute bottom-4 left-1/2 -translate-x-1/2 w-[88%]"><span class="btn-atelier-primary w-full flex justify-center">إضافة للسلة <i class="sicon-shopping-bag text-[16px] opacity-80"></i></span></div>
                </div>
                <div class="px-1 pt-4 flex flex-col flex-1">
                    <h3 class="text-[16px] font-bold leading-snug text-ink line-clamp-1">منتج أتيليه فاخر — Glass Demo {{ i }}</h3>
                    <p class="text-[13px] leading-5 text-gray-500 line-clamp-1 mt-1">تصميم زجاجي فاخر — يظهر القوة الحقيقية للثيم</p>
                    <div class="mt-3 flex items-end justify-between">
                        <span class="text-[20px] font-extrabold tracking-[-0.02em] text-ink leading-none">{{ (299 + i*12) }} ر.س</span>
                        <span class="flex items-center gap-1.5 bg-paper border border-outline px-2.5 py-1 rounded-full text-[12px] font-bold text-ink"><i class="sicon-star text-yellow-500 text-[11px]"></i> 5.0</span>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        <salla-products-slider source="latest" limit="8" class="hidden salla-fallback-real"></salla-products-slider>
        <p class="text-center text-[11px] tracking-wide text-gray-400 mt-4">عرض تجريبي زجاجي — سيُستبدل بمنتجات متجرك تلقائياً عند إضافة منتجات</p>
    {% endif %}
</section>
